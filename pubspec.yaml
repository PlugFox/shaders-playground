name: shimmer
description: "Shimmer"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0-42.2.beta <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  shaders:
    - assets/shaders/shimmer.frag
    - assets/shaders/rrect.frag
